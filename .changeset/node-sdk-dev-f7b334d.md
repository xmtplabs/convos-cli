---
"@xmtp/convos-cli": patch
---

Pin `@xmtp/node-sdk` and `@xmtp/node-bindings` to dev build `*-dev.f7b334d` (node-sdk `6.0.0-dev.f7b334d`, node-bindings `1.11.0-dev.f7b334d`). Widen the node-sdk/node-bindings peer ranges to `>=…-dev` so dev-channel prereleases satisfy the peer.
