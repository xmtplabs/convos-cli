# Release Workflow Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add automated npm release + PR CI for `@xmtp/convos-cli` using Changesets + GitHub Actions, with npm Trusted Publishing (OIDC).

**Architecture:** Two GitHub Actions workflows (`ci.yml` for PR/main gates, `release.yml` for Changesets Release-PR flow). Contributors add `.changeset/*.md` per user-facing PR. Bot opens "Version Packages" PR aggregating pending changesets; merging it publishes to npm + tags + GitHub Release. Existing handwritten `CHANGELOG.md` is preserved by prepending.

**Tech Stack:** GitHub Actions, Changesets (`@changesets/cli`, `@changesets/changelog-github`, `changesets/action@v1`), pnpm, Node 22, npm Trusted Publishing.

**Spec:** `docs/superpowers/specs/2026-05-29-release-workflow-design.md`

**Prerequisites (must be true before merging this plan's PR):**
- npm Trusted Publisher already configured for `xmtplabs/convos-cli` → workflow `release.yml`. ✅ Confirmed by user.
- Repo admin must, before/after merge: enable "Read and write" workflow permissions + "Allow GitHub Actions to create and approve PRs" (Settings → Actions → General). Add main branch protection requiring `ci.yml`. Documented in spec setup checklist.

---

## File Structure

**Files created:**
- `.github/workflows/ci.yml` — PR + push:main typecheck/test/build gate.
- `.github/workflows/release.yml` — push:main Changesets Release-PR + publish.
- `.changeset/config.json` — Changesets configuration.
- `.changeset/README.md` — Contributor instructions.

**Files modified:**
- `package.json` — add `release` script, `packageManager` field, dev deps.
- `pnpm-lock.yaml` — regenerated after dev dep install.
- `README.md` — add "Releases" section.

**Files deleted:**
- `package-lock.json` — single-lockfile cleanup; CI uses pnpm only.

Each task below is self-contained: produces a commit, leaves repo in a consistent state.

---

### Task 1: Delete redundant npm lockfile + pin pnpm

**Files:**
- Delete: `package-lock.json`
- Modify: `package.json` (add `packageManager` field)

Rationale: dual lockfiles drift. CI will use pnpm only. Pin the pnpm version so CI and contributors get identical behavior.

- [ ] **Step 1: Delete `package-lock.json`**

```bash
rm package-lock.json
```

- [ ] **Step 2: Add `packageManager` field to `package.json`**

Edit `package.json`. After the `"engines"` block, add a sibling `"packageManager"` field. Current local pnpm version is `10.33.0` (verified via `pnpm --version`). Use a SHA256-pinned form generated by `corepack`:

```bash
corepack use pnpm@10.33.0
```

That command will update `package.json` with a `"packageManager": "pnpm@10.33.0+sha512.<hash>"` line. If `corepack` is not available, fall back to plain `"packageManager": "pnpm@10.33.0"` (no hash) — Actions' `pnpm/action-setup` accepts both.

- [ ] **Step 3: Verify install still works**

Run: `pnpm install --frozen-lockfile`

Expected: success, no changes to `pnpm-lock.yaml`.

- [ ] **Step 4: Commit**

```bash
git add package.json pnpm-lock.yaml
git rm package-lock.json
git commit -m "chore: pin pnpm and drop npm lockfile"
```

(Or jj equivalent: `jj desc -m "chore: pin pnpm and drop npm lockfile"` after editing.)

---

### Task 2: Add Changesets dev dependencies + config

**Files:**
- Modify: `package.json` (devDependencies + scripts)
- Modify: `pnpm-lock.yaml` (auto)
- Create: `.changeset/config.json`
- Create: `.changeset/README.md`

- [ ] **Step 1: Install Changesets dev deps**

```bash
pnpm add -D @changesets/cli @changesets/changelog-github
```

Expected: `package.json` `devDependencies` gains both packages; `pnpm-lock.yaml` updates.

- [ ] **Step 2: Initialize Changesets**

```bash
pnpm changeset init
```

This creates `.changeset/config.json` and `.changeset/README.md` with defaults.

- [ ] **Step 3: Replace `.changeset/config.json` with project config**

Overwrite `.changeset/config.json` with:

```json
{
  "$schema": "https://unpkg.com/@changesets/config@3.0.0/schema.json",
  "changelog": [
    "@changesets/changelog-github",
    { "repo": "xmtplabs/convos-cli" }
  ],
  "commit": false,
  "fixed": [],
  "linked": [],
  "access": "public",
  "baseBranch": "main",
  "updateInternalDependencies": "patch",
  "ignore": []
}
```

- [ ] **Step 4: Append project guidance to `.changeset/README.md`**

Append to the bottom of the file (keep the Changesets boilerplate at the top):

```markdown

## Project conventions

- Run `pnpm changeset` before merging any user-facing PR.
- Pick the bump level (`patch`/`minor`/`major`) and write the changelog entry in the same narrative voice as existing `CHANGELOG.md` entries: full sentences, explain *why*, call out breaking changes prominently.
- Renovate/dependency-bump PRs typically skip changesets — they batch silently into the next real release.
- Releases ship by merging the bot-maintained "Version Packages" PR.
```

- [ ] **Step 5: Add `release` script to `package.json`**

In `package.json` `"scripts"` block, add:

```json
"release": "pnpm build && changeset publish"
```

(Place it alphabetically between `"prepack"` and `"start"`.)

- [ ] **Step 6: Verify `pnpm changeset --help` runs**

Run: `pnpm changeset --help`

Expected: Changesets help output, no errors.

- [ ] **Step 7: Commit**

```bash
git add package.json pnpm-lock.yaml .changeset/
git commit -m "chore: add Changesets config"
```

---

### Task 3: Add CI workflow

**Files:**
- Create: `.github/workflows/ci.yml`

- [ ] **Step 1: Create `.github/workflows/ci.yml`**

```yaml
name: CI

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm typecheck

      - name: Test
        run: pnpm test

      - name: Build
        run: pnpm build
```

Notes:
- `pnpm/action-setup@v4` reads the `packageManager` field from `package.json` to pick the pnpm version. No explicit `version:` input needed.
- `cache: pnpm` requires `setup-node` to run after `pnpm/action-setup` (which it does here).
- No matrix yet — single Node 22 job, matches `engines.node`.

- [ ] **Step 2: Validate YAML locally**

Run: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"`

Expected: no output (valid YAML). If Python is unavailable, skip and rely on GitHub's validation.

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: add typecheck/test/build workflow"
```

---

### Task 4: Add release workflow

**Files:**
- Create: `.github/workflows/release.yml`

- [ ] **Step 1: Create `.github/workflows/release.yml`**

```yaml
name: Release

on:
  push:
    branches: [main]

concurrency:
  group: release-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write       # tag + GitHub Release
  pull-requests: write  # Version PR
  id-token: write       # npm Trusted Publishing (OIDC) + provenance

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm typecheck

      - name: Test
        run: pnpm test

      - name: Build
        run: pnpm build

      - name: Create Release PR or Publish
        uses: changesets/action@v1
        with:
          publish: pnpm release
          version: pnpm changeset version
          title: "chore: release"
          commit: "chore: release"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

Notes:
- No `NPM_TOKEN` env. Trusted Publishing handles auth via `id-token: write` + `registry-url`.
- `changesets/action` runs `version` when no version commit is on HEAD (opens/updates Version PR). When HEAD is a version commit (post-Version-PR-merge), it runs `publish`.
- `concurrency.cancel-in-progress: false` — never abort a publish mid-flight.

- [ ] **Step 2: Validate YAML locally**

Run: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"`

Expected: no output.

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/release.yml
git commit -m "ci: add release workflow with Changesets and npm OIDC"
```

---

### Task 5: Document the release process in README

**Files:**
- Modify: `README.md` (append "Releases" section near the end)

- [ ] **Step 1: Find insertion point**

Open `README.md`. Append the new section just before the License section (or at the end if no License section exists in the file).

- [ ] **Step 2: Add Releases section**

```markdown
## Releases

Releases are automated via [Changesets](https://github.com/changesets/changesets) and GitHub Actions.

**For contributors:**

1. After making a user-facing change, run `pnpm changeset` in your PR branch.
2. Pick the bump level (`patch` / `minor` / `major`) and write the changelog entry in the same narrative voice as existing `CHANGELOG.md` entries — full sentences, explain *why*, call out breaking changes.
3. Commit the generated `.changeset/*.md` file with your PR.

Renovate and other dependency-bump PRs typically skip the changeset step — those bumps batch silently into the next real release.

**For maintainers:**

When PRs with changesets land on `main`, the `Release` workflow opens (or updates) a `chore: release` PR that aggregates all pending changesets, bumps `package.json`, and prepends `CHANGELOG.md`. Merging that PR triggers the workflow again to publish to npm (via Trusted Publishing / OIDC), tag the release, and create a GitHub Release.

To trigger a release manually after fixing a stuck publish, re-run the `Release` workflow from the Actions tab — `changeset publish` is idempotent.
```

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "docs: document release process"
```

---

### Task 6: Smoke-test Changesets locally (no publish)

This task is a verification gate — no files change. It confirms the tooling works end-to-end before the workflow runs in CI.

- [ ] **Step 1: Create a throwaway changeset**

```bash
pnpm changeset
```

In the interactive prompt:
- Select `@xmtp/convos-cli`.
- Bump type: `patch`.
- Summary: `Test changeset, do not merge.`

Expected: a new `.changeset/<random-name>.md` file appears.

- [ ] **Step 2: Run `version` dry-run**

```bash
pnpm changeset version
```

Expected:
- `package.json` version bumps by patch (e.g. `0.10.2` → `0.10.3`).
- `CHANGELOG.md` gets a new entry prepended at the top, leaving 0.10.2 and earlier untouched.
- The throwaway `.changeset/<random-name>.md` is deleted.

- [ ] **Step 3: Inspect the diff**

Run: `git diff` (or `jj diff`).

Verify:
- Only `package.json` version line + `CHANGELOG.md` change.
- Existing CHANGELOG entries are preserved verbatim.
- New entry includes a PR-link placeholder (will be empty since no PR exists locally — that's fine).

- [ ] **Step 4: Revert all changes**

```bash
git restore package.json CHANGELOG.md
rm -f .changeset/*.md
# Then re-apply the project's permanent .changeset/README.md and config.json if they were touched.
git restore .changeset/
```

(With jj: `jj restore package.json CHANGELOG.md .changeset/` then `rm` any new untracked files.)

Expected: working copy returns to the post-Task-5 state. `git status` shows clean (aside from any expected staged work).

- [ ] **Step 5: No commit — verification only**

This task creates no commit. Confirm the working tree is clean before proceeding.

---

### Task 7: Open PR, merge, verify first real release

This task spans the PR review + first production release. Treat the steps as a checklist for the maintainer doing the rollout.

- [ ] **Step 1: Push branch, open PR**

```bash
# git
git push -u origin <branch>
gh pr create --title "ci: automated releases via Changesets" --base main
```

(jj equivalent: `jj bookmark create <name>` → `jj git push -b <name>` → `gh pr create`.)

PR body should reference the spec:

```
Implements docs/superpowers/specs/2026-05-29-release-workflow-design.md
```

- [ ] **Step 2: Confirm `ci.yml` runs and passes on the PR**

Expected: a single `CI / build` check, green.

- [ ] **Step 3: Repo admin enables Actions permissions**

Repo Settings → Actions → General:
- Workflow permissions: **Read and write permissions**.
- Check: **Allow GitHub Actions to create and approve pull requests**.

(Required before `release.yml` can open the Version PR.)

- [ ] **Step 4: Merge the PR**

After approval, merge. `release.yml` will run on `main`. Since this PR contains no `.changeset/*.md`, the action will no-op (no Version PR opened, nothing published). This confirms the workflow is wired up safely.

Expected: `release.yml` run succeeds, logs say "No changesets found" or similar.

- [ ] **Step 5: Add branch protection on `main`**

Repo Settings → Branches → Add rule for `main`:
- Require `CI / build` to pass.
- Keep PR review requirement.

- [ ] **Step 6: First real release validation**

On the next user-facing PR that lands with a `.changeset/*.md`:
- After merge, watch `release.yml` open a `chore: release` PR within ~1 minute.
- Review the diff: `package.json` bump, `CHANGELOG.md` prepend, changesets deleted.
- Merge the Version PR.
- Watch `release.yml` run a second time. Expected output:
  - `pnpm release` runs, `changeset publish` calls `pnpm publish`.
  - npm publish succeeds via OIDC (no `NPM_TOKEN` warnings).
  - Git tag `vX.Y.Z` created.
  - GitHub Release created with changelog body.
- Verify: `npm view @xmtp/convos-cli` shows new version and provenance attestation.

- [ ] **Step 7: No commit — production verification**

This task verifies the system end-to-end. No code changes.

---

## Self-review notes

**Spec coverage check:**
- Goal (automated release, narrative CHANGELOG, tags, GitHub Releases, OIDC, PR CI) → Tasks 2–4, 6, 7. ✅
- Architecture (two workflows + `.changeset/` + lockfile cleanup) → Tasks 1–4. ✅
- Existing CHANGELOG preserved → verified in Task 6 step 3. ✅
- Renovate/dep PRs skip changesets → documented in Tasks 2 + 5. ✅
- Trusted Publishing setup checklist (admin perms, branch protection) → Task 7 steps 3, 5. ✅
- Out-of-scope items (lint CI, env gates, enforced changesets) stay out. ✅

**Placeholder scan:** no TBDs, no "add appropriate X", every code block is complete content.

**Type/name consistency:** workflow names (`ci.yml`, `release.yml`), script name (`release`), action ref (`changesets/action@v1`), package names (`@changesets/cli`, `@changesets/changelog-github`) — used identically across tasks.

**Reversibility:** Task 1 is reversible (restore lockfile from git). Tasks 2–5 are pure additions. Task 6 explicitly reverts itself. Task 7 production steps are gated by maintainer + PR review.
